# はじめかた（Mac用・ステップバイステップ）

前提：macOS 13 以上。Claude の有料プラン（Pro / Max / Team など）が必要です。
無料プランでは Claude Code は使えません。

---

# ルートA：黒い画面を使わない方法（おすすめ）

デスクトップアプリを使えば、ターミナルを開かずに同じことができます。
はじめてならこちらでどうぞ。

## A-1. アプリをダウンロードする

下のリンクを Safari か Chrome で開きます。`.dmg` というファイルが落ちてきます。

https://claude.ai/api/desktop/darwin/universal/dmg/latest/redirect

## A-2. インストールする

1. ダウンロードした `.dmg` をダブルクリック
2. 窓が開くので、**Claude のアイコンを Applications フォルダにドラッグ**
3. 「アプリケーション」から Claude を起動
4. 「開いてもよろしいですか？」と出たら「開く」

## A-3. ログインする

アプリが開いたら、いつも使っている Claude のアカウントでログインします。

## A-4. サイトのフォルダを用意する

1. Finder を開く
2. 左側の「書類」をクリック
3. お渡ししたフォルダ（`repo`）を、この「書類」の中にドラッグして入れる
4. 名前を `sukajan-site` などに変えておくと分かりやすいです

## A-5. アプリでフォルダを開く

1. Claude アプリの上のほうにある **Code** を開く
2. 「フォルダを開く」「プロジェクトを選ぶ」といったボタンから、
   さきほどの `書類 → sukajan-site` を選ぶ

画面の案内に沿って進めば大丈夫です。詳しい手順は公式にもあります。
https://code.claude.com/docs/en/desktop-quickstart

## A-6. 話しかける

あとは入力欄に日本語で打つだけです。

```
このサイトの構成を教えて
```

まずはこれを打ってみてください。ファイルを読んで説明してくれます。
**読むだけなので、何も壊れません。** 感触をつかんでから次に進みましょう。

→ 「ふだんの使い方」（このページの下のほう）へ

---

# ルートB：ターミナルを使う方法

黒い画面に慣れている方はこちら。5分で終わります。

## B-1. フォルダを置く

Finder で「書類」を開き、お渡ししたフォルダを入れます。
名前は `sukajan-site` にしておきます。

## B-2. ターミナルを開く

1. キーボードで **⌘（コマンド）＋ スペース** を押す
2. `ターミナル` と打って **Enter**

白か黒の窓が開きます。これがターミナルです。

## B-3. Claude Code を入れる

ターミナルに、次の1行を貼り付けて **Enter**。

```
curl -fsSL https://claude.ai/install.sh | bash
```

文字がたくさん流れます。終わるまで1〜2分待ちます。

## B-4. ターミナルを開き直す

**⌘ + N** で新しい窓を開きます。
（入れたばかりのものは、開き直さないと認識されません。ここでつまずく人が多いです）

新しい窓で、次を打って **Enter**。

```
claude --version
```

`2.1.211 (Claude Code)` のように数字が出れば成功です。

> `command not found` と出たら → B-3をやり直すか、`claude doctor` と打つと原因を教えてくれます。

## B-5. フォルダに移動する

ターミナルに `cd ` と打ちます。**cd のあとに半角スペースを1つ**入れてください。

そのまま、Finder の `sukajan-site` フォルダを**ターミナルの窓にドラッグ&ドロップ**します。
パスが勝手に入るので、**Enter**。

```
cd /Users/あなたの名前/Documents/sukajan-site
```

こうなっていればOKです。

## B-6. 起動する

```
claude
```

と打って **Enter**。初回はブラウザが開くので、ログインします。
戻ってくると、入力待ちの状態になっています。

## B-7. 話しかける

```
このサイトの構成を教えて
```

読むだけの指示なので安全です。まずはこれで感触をつかんでください。

---

# ふだんの使い方

ルートA・Bどちらでも、ここからは同じです。日本語で話しかけるだけ。

## 掲載記事が増えた

```
/press 2026年10月1日、Casa BRUTUSに載りました。
タイトルは「横須賀の絵師」。URLは https://～
```

## 展示会の告知を出す

```
/notice 10月に横浜で展示会に出ます
```

終わったら

```
/notice 消して
```

## 作品を1件足す

```
作品を追加して。名前は「鳳凰と桜」、フルオーダーの一着、
横振り刺繍、レーヨン、制作1ヶ月
```

## 文章を直す

```
トップページの最初の説明文を、もう少し短くして
```

## サイトに反映する

```
/publish
```

おかしなところがないか調べたあと、**「公開していい？」と聞いてきます。**
「はい」と答えるまでサイトは変わりません。

---

# つまずいたときは

**そのまま日本語で聞いてください。** これがいちばん早いです。

```
いまエラーが出た。どうすればいい？
```

```
さっきの変更を取り消して
```

```
いま何が変わってるの？
```

## よくあるつまずき

| 症状 | 対処 |
|---|---|
| `command not found: claude` | ターミナルを新しく開き直す（⌘+N）。それでもダメなら `claude doctor` |
| どのフォルダにいるか分からない | `pwd` と打つと今いる場所が出ます |
| フォルダを間違えた | `cd ` のあとにフォルダをドラッグ&ドロップし直す |
| 変なことになった | ターミナルの窓を閉じて開き直せば元通り。ファイルは壊れません |

---

# 最初にやることリスト

- [ ] アプリを入れる（ルートA）か、ターミナルで入れる（ルートB）
- [ ] `書類` にフォルダを置く
- [ ] 起動して「このサイトの構成を教えて」と打つ
- [ ] `/notice テスト` と打って、告知が出るか試す
- [ ] `/notice 消して` で戻す

ここまでできれば、あとは同じことの繰り返しです。
GitHub とサーバーへの接続は、慣れてからで大丈夫です（`UPDATE.md` に書いてあります）。
