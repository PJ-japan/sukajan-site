# スカジャン絵師 横地広海知 — サイト

静的サイト。`build.py`（Python標準ライブラリのみ）が `pages/` と `content/` から
`dist/` にHTMLを書き出す。依存パッケージなし、Node不要。

## 更新するときのルール

**編集していいのはここだけ**

| 対象 | 用途 |
|---|---|
| `content/notice.json` | ヘッダー直下の告知帯（1行） |
| `content/press.json` | 掲載・出演・受賞 |
| `content/works.json` | 制作事例 |
| `pages/*.html` | 各ページの本文（`<main>` の中身だけ） |
| `assets/` | 画像 |

**確認なしに触らないもの**

- `build.py` — ヘッダー・フッター・ナビ・メタ情報・構造化データの生成。構成を変えるとき以外は触らない
- `_style.css` — デザイン。色や書体の変更は必ず相談してから
- `_doc.js` — **仕様確認書・発注確認書の条項（著作権3項・許諾条件4項・禁止事項11項）。変更は必ず本人の明示的な指示がある場合のみ**
- `.github/workflows/` — デプロイ設定

## 作業の手順

1. `content/` または `pages/` を編集
2. `python3 build.py` を実行
3. `python3 -m http.server -d dist 8000` で確認（必要なら）
4. 変更内容を1行で説明してコミット
5. `git push`（GitHub Actions が自動でビルドして公開する）

ビルドが通らない、`dist/index.html` に `<h1>` が無い場合は push しない。

## 書き方の決まり

- 見出しの欧文は `<span class="en">`、日本語は小さく添える
- セクションラベルは `<p class="tag"><span class="tag__en">ENGLISH</span><span class="tag__ja">日本語</span></p>`
- カードで囲まない。罫線だけの `.rows > .row` を使う
- 番号付き（`.seq`）は順序に意味がある工程のときだけ
- 未確定の数値は `<span class="tbd">…</span>` で囲む。公開前に埋める前提
- 色は近黒・オフホワイト・朱の3色のみ。新しい色を足さない
- 競合他社を名指しで批判する文言は書かない
- **スカジャンの柄を「和柄」と呼ばない。**「和柄」はもっと現代的な装飾の呼び方で、
  浮世絵をスカジャンに載せるようなズレが生じる。スカジャンの柄は、スカジャンに
  載せるために選ばれ練られてきた柄なので、単に「柄」「スカジャンの柄」と書く

## 事例を足すとき

`content/works.json` に追加。`image` を空にすると点線のプレースホルダになる。
個別ページを作る場合は `pages/works-{slug}.html` を作り、`build.py` の `PAGES` に登録する。
title は「柄の名前」を必ず含める（検索流入の主戦場）。

## 掲載を足すとき

`content/press.json` の**先頭**に追加。年の見出しは自動で作られる。
`url` を空文字にするとリンクなしの行になる（掲載制約がある場合）。

## 告知

`content/notice.json` の `enabled` を `true` に。
**告知が終わったら必ず `false` に戻す。** 古い告知の出しっぱなしが一番よくない。

## brief.html と spec.html は非公開ページ

どちらもヘッダーのナビ、フッター、`sitemap.xml` には**出さない**。`noindex` も付けている。
URLを直接開けば使える。`build.py` の `PAGES[...]` にある `unlisted` と `noindex` は外さない。
公開ページから `href="brief.html"` でリンクしないこと（外から辿れてしまう）。

| | 用途 | 渡し方 |
|---|---|---|
| `brief.html` | クライアントが答えるヒアリングフォーム | 相談が始まってからURLを個別に案内する |
| `spec.html` | 受注者側の業務用ツール（金額・支払条件・改訂履歴） | 社内用 |

`brief` で「JSONで保存」したファイルを `spec` の「JSONを読み込み」で取り込んで仕上げる。

## 店舗情報は access.html に集約

ICHIドブ板本店の住所・営業時間・行き方は `pages/access.html` が正。
`contact.html` と `oem.html` には要約だけ置き、詳細は access へリンクする。
同じ情報を3ページに書くと、更新漏れが起きるうえ検索側でも評価が割れる。
`LocalBusiness` の構造化データは access / contact / index / oem に出しており、
定義元は `build.py` の `LOCAL`。店名は「ICHIドブ板本店」。

## ヘッダーのナビ

`build.py` の `SHELL` にハンバーガー用のインラインスクリプトがある。
880px以下でボタンを出し、nav を開閉する。JSが動かない環境ではボタンが出ず、
nav が横スクロールで残るフォールバック。`data-js` と `data-open` 属性で
CSS 側と連動しているので、どちらかだけ変えない。

**`SHELL` は `.format()` を通る。** スクリプトを足すときは波括弧を `{{` `}}` に
エスケープしないとビルドが落ちる。

## 書類は2種類ある。混同しない

| | 何か | 弁護士レビュー |
|---|---|---|
| **契約書** | 実際の取引で交わす書面。このリポジトリには入っていない | **済** |
| **仕様確認書・発注確認書** | `_doc.js` が生成する草案。相談・社内検討用 | 未（README の公開前TODO） |

サイトの文面で「顧問弁護士監修」と書けるのは**契約書のほう**だけ。
`brief.html` / `spec.html` が自動生成するのは仕様確認書であって契約書ではないので、
両者を同じものとして書かない。`spec.html` の免責文（専門家のレビューを受けてください）は、
仕様確認書についての注意書きなので消さない。

## 未確定の内容は HOLD で囲む

まだ埋まっていない箇所は、削除せず `<!--HOLD-->` 〜 `<!--/HOLD-->` で囲む。
`build.py` の `hold()` が出力から取り除くので、ソースには残ったまま公開版には出ない。
内容が決まったら囲みを外すだけで復活する。

現在保留しているもの:

| 場所 | 内容 | 外す条件 |
|---|---|---|
| `index.html` | 制作事例のカード列 | `content/works.json` に実データと画像が入ったら |
| `works.html` | 最近の事例のカード列、運用メモ | 同上／運用メモは不要になったら削除 |
| `order.html` | サイズ表、柄の画像枠、制作動画枠 | 実寸と写真が揃ったら |
| `about.html` | 作画風景の画像枠 | 写真が揃ったら |
| `access.html` | 営業時間・定休日・電話・駐車場 | 確定したら |
| `press.html` | 運用メモ | 不要になったら削除 |

`class="tbd"` は作業用の目印。**公開版に残してはいけない。** 現在0件。

## 触るときに気をつけること

- パスワード・APIキー・FTP情報はリポジトリに書かない。GitHub の Secrets に入れる
- **オリンピック関連は権利上いっさい掲載できない。** 東京五輪の実績は本人の指示で全削除済み。
  「東京五輪」「TOKYO OLYMPICS」「五輪」を、実績表・ティッカー・本文・meta のどこにも書かない
- 実績（GU / PUMA / 大阪関西万博 / モンスターハンター / 徳川美術館 / 還ジャン&reg;）の掲載可否は案件ごとに異なる。
  新しく実績を足すよう指示された場合、掲載許諾を確認済みか一度確認する
- `brief.html` は入力内容が外部（Googleスプレッドシート）へ送信される。
  項目を増やすときはプライバシーポリシーの記載と整合しているか確認する
