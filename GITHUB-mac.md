# GitHubの準備（Mac用）

## その前に：今すぐ必要ではありません

GitHubがなくても、サイトの編集はできます。
Claude Code に頼めばファイルを直してビルドまでやりますし、
できあがった `dist/` の中身をFTPソフトでサーバーに上げれば公開できます。

GitHubを入れる理由は3つです。

1. **失敗しても戻せる**（過去の全部の版が残る）
2. **自動で公開される**（保存 → 数十秒でサイトに反映）
3. **スマホからでも触れる**

先にアプリの操作に慣れて、あとから足すのでも大丈夫です。

---

## 用意するもの

| 必要なもの | どこで手に入るか | 費用 |
|---|---|---|
| GitHubアカウント | github.com | 無料 |
| メールアドレス | お手持ちのもの | — |
| スマホ（2段階認証用） | お手持ちのもの | — |
| サーバーのFTP情報 | サーバー会社の管理画面 | — |

Git本体のインストールは要りません。Macに最初から入っています。
（入っていなければ、初回に「インストールしますか」と出るので「インストール」を押せばOK）

---

## ステップ1：アカウントを作る（5分）

1. https://github.com を開く
2. 右上の **Sign up** をクリック
3. メールアドレス、パスワード、ユーザー名を入れる
   - ユーザー名は英数字。`yokochi-hiromichi` のような形で
4. 届いたメールの数字を入力

## ステップ2：2段階認証を設定する（5分）

いまのGitHubは必須です。避けて通れません。

1. 画面の案内に沿って進む
2. スマホの認証アプリ（Google Authenticator など）か、SMSを選ぶ
3. **回復コードが表示されるので、必ず保存してください**
   （スマホを失くしたときの最後の手段です。印刷するか、パスワード管理ソフトへ）

## ステップ3：保管庫（リポジトリ）を作る（2分）

1. GitHubの右上の **+** → **New repository**
2. こう設定します

| 項目 | 設定 |
|---|---|
| Repository name | `sukajan-site` |
| Description | 空でOK |
| 公開範囲 | **Private**（非公開）を選ぶ |
| Add a README file | **チェックを外す** |
| .gitignore / license | どちらも None |

3. **Create repository** をクリック

次の画面に出るURLを控えておきます。

```
https://github.com/あなたのユーザー名/sukajan-site
```

## ステップ4：フォルダをアップする（Claude Codeに任せる）

Claude Code を開いて、こう打ちます。

```
このフォルダをGitHubの sukajan-site リポジトリにアップしてください。
URLは https://github.com/あなたのユーザー名/sukajan-site です。
```

途中でいくつか聞かれます。

- **名前とメールアドレス** … GitHubで登録したものを答える
- **ログイン** … ブラウザが開くので、GitHubにログインして許可する

**パスワードやトークンをチャットに貼らないでください。**
ブラウザで認証する方式なら、そもそも貼る必要がありません。

うまくいくと、GitHubのページを再読み込みしたときにファイルが並んでいます。

## ステップ5：公開先をつなぐ（10分）

ここまでで保管庫はできました。次は「保存したら自動でサイトに反映」の設定です。

### 5-1. FTP情報を用意する

サーバー会社（GMOなど）の管理画面にログインして、次の4つをメモします。

| 項目 | 例 |
|---|---|
| FTPサーバー名（ホスト） | `ftp.example.com` |
| FTPユーザー名 | `example-user` |
| FTPパスワード | ******** |
| 公開ディレクトリ | `/public_html/` |

### 5-2. GitHubに登録する

1. リポジトリのページで **Settings**（歯車のタブ）
2. 左メニューの **Secrets and variables** → **Actions**

**Variables タブ**で **New repository variable** を押し、1つ登録します。

| Name | Value |
|---|---|
| `DEPLOY_TARGET` | `ftp` |

**Secrets タブ**に戻り、**New repository secret** で4つ登録します。

| Name | Value |
|---|---|
| `FTP_HOST` | ftp.example.com |
| `FTP_USER` | ユーザー名 |
| `FTP_PASSWORD` | パスワード |
| `FTP_DIR` | /public_html/ |

> Secretsに入れた値は、あとから中身を見ることができません（それが正しい動作です）。
> パスワードは手元にも控えておいてください。

## ステップ6：動くか試す

Claude Code に打ちます。

```
/publish
```

確認を聞かれるので「はい」と答えると、GitHubに送られます。
そのあと、GitHubのページの **Actions** タブを開いてください。

- **黄色い丸** … 作業中（30秒〜1分）
- **緑のチェック** … 成功。サイトに反映されています
- **赤いバツ** … 失敗。クリックすると理由が出ます

赤くなったら、その画面の文字をコピーして Claude Code に貼れば、原因を調べてくれます。

---

## つまずきやすいところ

| 症状 | 原因と対処 |
|---|---|
| `Permission denied` | ログインし直す。Claude Code に「GitHubにログインし直したい」と伝える |
| Actionsが動かない | リポジトリの **Actions** タブで、有効化のボタンが出ていないか確認 |
| 赤いバツになる | たいていFTP情報の打ち間違い。Secretsを登録し直す（上書きできます） |
| ファイルが上がっていない | Claude Code に「今の状態を教えて」と聞く |

---

## やってはいけないこと

- **パスワードやAPIキーをファイルに書かない。** 必ず Secrets に入れる
- **リポジトリを Public にしない。** 今回は Private で作ってください
- **回復コードを失くさない。** スマホを機種変更したときに詰みます

---

## 覚えておく言葉

| 言葉 | 意味 |
|---|---|
| リポジトリ | GitHub上のフォルダ |
| コミット | ここまでの変更を記録する（セーブ） |
| プッシュ | 記録をGitHubに送る |
| Actions | GitHubが自動でやってくれる作業。今回はビルドと公開 |
| Secrets | パスワードを安全にしまっておく場所 |
